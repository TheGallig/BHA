$(window).on('resize', function () {
    $('.halfwidth1').width($(window).width() / 4);
}).resize();

$(window).on('resize', function () {
    $('.halfwidth2').width($(window).width() / 4);
}).resize();

$(window).on('resize', function () {
    $('.halfwidth3').width($(window).width() / 4);
}).resize();

$(window).on('resize', function () {
    $('a').width($(window).width() / 4);
}).resize();